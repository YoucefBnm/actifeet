import { Spinner } from '.'

const Loader = () => {
  return (
    <div className="w-screen h-dvh flex flex-col gap-4 justify-center items-center">
        <Spinner />
    </div>
  )
}

export default Loader